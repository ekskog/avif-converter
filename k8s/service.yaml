apiVersion: v1
kind: Service
metadata:
  name: avif-converter
  namespace: webapps
  labels:
    app: avif-converter
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: avif-converter
